/* global angular */
(function(ng) {
    'use strict';

    return ng
        .module('angular-cockpit', [
            'angular-cockpit.factories',
            'angular-cockpit.forms'
        ]);

})(angular);
